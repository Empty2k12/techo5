# Alpine packages apk installs into the persistent root filesystem
# (tools/linux/mkrootfs.sh), from the repositories the minirootfs points at
# (v3.24 main + community). One per line; # starts a comment.
dropbear
dropbear-scp
iw
wireless-tools
e2fsprogs
tzdata
alsa-utils
# wpa_supplicant 2.9 comes from local v3.12 packages (see README.md, "Why these
# choices"); apk resolves its libnl3 / dbus-libs / pcsc-lite-libs dependencies
# from v3.24 and its libssl1.1 / libcrypto1.1 ones from the same local set.

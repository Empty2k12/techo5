//go:build !dot

package home

// hasScreen is whether this device has a screen a screen-only feature (the slideshow) could show
// on. True for the Show and the Spot; the Dot has none.
const hasScreen = true
